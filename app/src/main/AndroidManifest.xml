<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deneme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".fight_temp"></activity>
        <activity android:name=".sehir" />
        <activity android:name=".yetenek" />
        <activity android:name=".dovusdeneme" />
        <activity android:name=".info" />
        <activity android:name=".stat" />
        <activity android:name=".olusturma" />
        <activity android:name=".atolye" />
        <activity android:name=".uret" />
        <activity android:name=".anaekran" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>